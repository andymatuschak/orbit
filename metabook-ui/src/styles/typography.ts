import colors from "./colors";

const fontFamily = "Libre Franklin";

export default {
  cardBodyText: {
    fontFamily,
    fontSize: 18,
    lineHeight: 1.625 * 18,
    color: colors.textColor
  }
}