{
  "private": true,
  "scripts": {
    "build": "tsc -b packages/tsconfig.json && (cd packages/backend; yarn build:local)",
    "watch": "tsc -b -w packages/tsconfig.json",
    "test": "cd packages; jest --projects anki-import api-client ingester core web-component api store-fs store-web interpreter; cd backend; yarn run test",
    "lint": "cd packages; eslint .",
    "lint:fix": "cd packages; eslint --fix .",
    "checkall": "yarn run lint && yarn run build && yarn run test"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "eslint": "^8.55.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-prettier": "^5.0.1",
    "eslint-plugin-react": "^7.33.2",
    "eslint-plugin-react-hooks": "^4.6.0",
    "prettier": "^2.3.1",
    "typescript": "^5.3.3"
  },
  "trustedDependencies": [
    "sqlite3",
    "remark-wiki-link"
  ],
  "workspaces": [
    "packages/*"
  ],
  "name": "@withorbit/umbrella"
}
